services:
  - type: web
    name: skillswap-api
    env: docker
    plan: free
    region: oregon
    dockerfilePath: ./SkillSwap/Dockerfile
    envVars:
      - key: SERVER_PORT
        value: "8080"
      - key: MONGODB_URI
        sync: false
